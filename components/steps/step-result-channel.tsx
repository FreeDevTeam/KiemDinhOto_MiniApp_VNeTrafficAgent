"use client"
import type { InspectionData, ResultChannel } from "@/lib/inspection"
export function StepResultChannel({ data, update, onSuccess }: { data: InspectionData; update: (patch: Partial<InspectionData>) => void; onSuccess: () => void }) { return <div className="flex flex-col gap-5"><header className="flex flex-col gap-1.5"><h1 className="text-2xl font-bold">Cách nhận kết quả</h1><p className="text-sm text-muted-foreground">Chọn cách nhận thông báo khi có kết quả tra cứu.</p></header><section className="flex flex-col gap-4 rounded-xl border border-input bg-card p-5 shadow-sm"><h2 className="text-sm font-semibold">Cách nhận kết quả</h2><div className="flex flex-wrap gap-4">{([['telegram','Telegram'],['zalo','Zalo'],['sms','SMS']] as [ResultChannel,string][]).map(([value,label]) => <label key={value} className="flex items-center gap-2 text-sm font-medium"><input type="radio" name="result-channel" checked={data.resultChannel === value} onChange={() => update({ resultChannel: value })} className="size-4 accent-primary" />{label}</label>)}</div><label htmlFor="result-phone" className="text-sm font-semibold">Số điện thoại</label><input id="result-phone" value={data.phoneNumber} onChange={(e) => update({ phoneNumber: e.target.value.replace(/[^0-9]/g, "") })} inputMode="tel" className="h-12 rounded-xl border border-input px-4" /><button type="button" onClick={onSuccess} disabled={data.phoneNumber.trim().length < 9} className="h-12 rounded-xl bg-primary px-4 text-sm font-semibold text-primary-foreground disabled:cursor-not-allowed disabled:opacity-50">Tiếp tục</button></section></div> }
